package fr.forumhfr.redface2.core.domain.preferences

/** Light-theme surface policy applied after dynamic or seeded chromatic roles are resolved. */
enum class LightSurfaceTone {
    MATERIAL_TINTED,
    WHITE,
    REDFACE1_GRAY,
}
