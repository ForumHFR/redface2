Redface 2 v0.34.4 — dev.

- Image pass, error states: a dead image now shows a real error slot (no more silent empty box) and retries with a single "Retry" tap. Pull-to-refresh only reloads the images that actually failed. SVGs now render; AVIF follows your device decoder (clean error state otherwise).
