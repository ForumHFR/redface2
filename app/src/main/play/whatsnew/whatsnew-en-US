Redface 2 v0.34.5 — dev.

- New "Full-width posts" setting (Settings → Topic): posts render edge to edge, without the card inset — more room for text and images. Off by default: nothing changes unless you opt in.
- Accessibility: TalkBack heading navigation per post (author pseudo), in both modes.
