Redface 2 v0.34.3 — dev.

- Images pass, fine sizing: no more blur (decoding at the real on-screen size), no artificial upscaling — every image renders at its native size at most. GIFs only animate while visible on screen (battery friendly), and large photos no longer get their dimensions clipped.
