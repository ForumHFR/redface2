Redface 2 v0.42.5 — dev.

- Private conversations keep their pages in memory: returning to a page you already read is instant.
- Adjacent pages are prefetched while you read, only for the conversation on screen — never from the list, so an unread you never saw is not cleared.
- Nothing is written to disk: the cache is gone when the app closes and when you sign out.
